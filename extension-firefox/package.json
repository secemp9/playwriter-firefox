{
    "name": "playwriter-firefox-extension",
    "version": "0.0.1",
    "description": "Playwriter Firefox Browser Extension",
    "private": true,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/remorses/playwriter.git"
    },
    "homepage": "https://github.com/remorses/playwriter",
    "engines": {
        "node": ">=18"
    },
    "author": {
        "name": "Microsoft Corporation"
    },
    "license": "Apache-2.0",
    "scripts": {
        "build": "tsc --project . && vite build --config vite.config.mts",
        "watch": "vite build --watch --config vite.config.mts",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@types/webextension-polyfill": "^0.12.4",
        "typescript": "^5.8.2",
        "vite": "^5.4.21",
        "vite-plugin-static-copy": "^3.1.1",
        "webextension-polyfill": "^0.12.0"
    },
    "dependencies": {
        "@ffmpeg/ffmpeg": "^0.12.10",
        "@ffmpeg/util": "^0.12.1",
        "playwriter": "workspace:*"
    }
}
